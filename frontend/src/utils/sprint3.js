export async function authorizeSupplier(address) {
  console.log('Placeholder de autorização de fornecedor com Ethers.js', {
    address,
  })

  return {
    success: true,
  }
}

export async function withdrawFunds(amountInEth) {
  console.log('Placeholder de saque com Ethers.js', {
    amountInEth,
  })

  return {
    success: true,
  }
}
